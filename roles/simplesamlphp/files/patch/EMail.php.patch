Description: Remove mailto: from email address
 technicalcontact_email is used both in the metadata and for sending error
 reports to. Since we do want to include a mailto: for the metadata, we make
 sure we remove mailto: when using it as actual email adress. 
Author: Mischa Sallé <msalle@nikhef.nl>
--- a/lib/SimpleSAML/Utils/EMail.php	2019-12-09 17:03:39.000000000 +0100
+++ b/lib/SimpleSAML/Utils/EMail.php	2020-01-07 11:05:53.000000000 +0100
@@ -68,7 +68,13 @@
         if ('na@example.org' === $address) {
             throw new \Exception('technicalcontact_email must be changed from the default value');
         }
-        return $address;
+        // remove mailto: when present
+        $mailto = 'mailto:';
+        $len = strlen($mailto);
+        if (substr($address, 0, $len) === $mailto)
+            return substr($address, $len);
+        else
+            return $address;
     }
 
     
