---

- name: enable modules
  file:
    path: "{{ ssp_dir }}/modules/{{ item }}/enable"
    state: touch
  with_items: "{{ enabled_modules }}"

- name: disable modules
  file:
    path: "{{ ssp_dir }}/modules/{{ item }}/disable"
    state: touch
  with_items: "{{ disabled_modules }}"
